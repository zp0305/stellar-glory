# FEATURE-主页学科状态更新完成记录

## 一、任务概述

更新主页（SiteHome）的学科入口显示逻辑，使所有六大学科正确显示为可用状态。

## 二、修改的文件

### 2.1 src/sections/SiteHome.tsx

**主要改动：**
- 将硬编码的 `subjects` 数组替换为从 `@/data/subjects` 导入的 `SUBJECTS` 统一配置
- 创建 `iconMap` 映射表，为每个学科分配图标
- 创建 `subjectDescriptions` 映射表，更新各学科描述
- 创建 `subjectModuleCounts` 映射表，记录各学科模块数量
- 更新学科卡片渲染逻辑，使用统一数据源

**变更详情：**
| 学科 | 之前状态 | 现在状态 | 模块数 |
|:---|:---:|:---:|:---:|
| 物理 | ✅ 可用 | ✅ 可用 | 26 |
| 化学 | ❌ 即将上线 | ✅ 可用 | 60 |
| 数学 | ❌ 即将上线 | ✅ 可用 | 8 |
| 生物 | ❌ 即将上线 | ✅ 可用 | 52 |
| 语文 | ❌ 即将上线 | ✅ 可用 | 29 |
| 英语 | ❌ 即将上线 | ✅ 可用 | 52 |

**学科描述更新：**
- 物理：26个核心模型，从运动到电磁，构建完整认知体系
- 化学：元素周期、化学反应、有机化学，探索物质变化的奥秘
- 数学：函数、几何、概率，从具象到抽象的思维训练
- 生物：49个核心概念，52个解题模型，理解生命的本质与演化
- 语文：阅读写作、古诗文、语言运用，文化传承与表达能力
- 英语：49个核心概念，52个解题模型，走向世界的桥梁

## 三、技术实现

### 3.1 数据结构

```typescript
const iconMap: Record<string, React.ComponentType> = {
  physics: FlaskConical,
  chemistry: Atom,
  biology: Dna,
  math: Calculator,
  chinese: BookOpen,
  english: Languages,
}
```

### 3.2 渲染逻辑

学科卡片现在从 `SUBJECTS` 数组动态渲染，确保与 `src/data/subjects.ts` 配置保持一致。

## 四、验证结果

- ✅ TypeScript 编译通过（0错误）
- ✅ 构建成功
- ✅ 所有六学科正确显示为可用状态

## 五、未完成/遗留事项

- 学科卡片的学习进度功能暂未实现（当前显示进度为100%）
- 除物理外，其他学科的题库统计数据尚未接入

## 六、与项目经理对齐状态

- 状态：已完成
- 待审核：是

---

**创建时间**：2026-04-29  
**文件版本**：v1.0